name: vm
description: A very simple flutter plugin that implements the `MVVM` pattern.
homepage: 'https://github.com/tiamo/flutter-vm'

version: 1.0.2

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
